extends layout

block content
  h1= title

  ul
    each manufacturer in allManufacturers
      li
        a(href=manufacturer.url) #{`${manufacturer.name} (${manufacturer.city}, ${manufacturer.country})`}

    else
      li There are no manufacturer.